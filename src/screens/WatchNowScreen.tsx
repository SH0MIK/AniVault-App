import React,{useCallback,useEffect,useState}from'react';import{View,Text,FlatList,Pressable,ActivityIndicator,RefreshControl,StyleSheet}from'react-native';import{Ionicons}from'@expo/vector-icons';import{useNavigation}from'@react-navigation/native';import{getWatchNow,MiniAnimeCard}from'../api/content';import{WebHeader,WebSectionHeader,WebAnimeCard,WebFooter}from'../components/WebChrome';import{colors,fonts}from'../theme';
export default function WatchNowScreen(){const navigation=useNavigation<any>();const[items,setItems]=useState<MiniAnimeCard[]>([]);const[page,setPage]=useState(1);const[total,setTotal]=useState(1);const[loading,setLoading]=useState(true);const[refreshing,setRefreshing]=useState(false);const load=useCallback(async(p=1,append=false)=>{setLoading(true);try{const r=await getWatchNow(p);setItems(x=>append?[...x,...r.data]:r.data);setTotal(r.totalPages);setPage(p)}finally{setLoading(false)}},[]);useEffect(()=>{load()},[load]);const refresh=async()=>{setRefreshing(true);await load(1);setRefreshing(false)};return <View style={s.root}><WebHeader navigation={navigation} routeName="WatchNow"/><FlatList data={items} keyExtractor={x=>String(x.id)} numColumns={3} columnWrapperStyle={s.row} contentContainerStyle={s.content} refreshControl={<RefreshControl refreshing={refreshing} onRefresh={refresh} tintColor={colors.accent}/>} ListHeaderComponent={<><View style={s.hero}><Text style={s.kicker}>YOUR VAULT</Text><Text style={s.heading}>Watch Now</Text><Text style={s.sub}>Discover anime ready to watch.</Text></View><WebSectionHeader title="Latest releases"/></>} renderItem={({item})=><WebAnimeCard title={item.title} image={item.image} score={item.score} type={item.type} episodes={item.episodes} onPress={()=>navigation.navigate('AnimeDetail',{id:item.id,title:item.title})} width={105}/>} onEndReached={()=>{if(!loading&&page<total)load(page+1,true)}} onEndReachedThreshold={.5} ListFooterComponent={<>{loading?<ActivityIndicator color={colors.accent} style={{margin:18}}/>:null}<WebFooter/></>} ListEmptyComponent={!loading?<View style={s.empty}><Ionicons name="play-circle-outline" size={44} color={colors.textMuted}/><Text style={s.emptyTitle}>Nothing to watch yet</Text><Text style={s.emptyText}>Check back soon for new releases.</Text></View>:null}/></View>};const s=StyleSheet.create({root:{flex:1,backgroundColor:colors.bgBase},content:{paddingBottom:0},hero:{padding:18,backgroundColor:colors.bgSurface,borderBottomWidth:1,borderBottomColor:colors.border},kicker:{color:colors.accent,fontFamily:fonts.displayMedium,fontSize:8,letterSpacing:1.5},heading:{color:colors.textPrimary,fontFamily:fonts.display,fontSize:21,marginTop:5},sub:{color:colors.textMuted,fontFamily:fonts.body,fontSize:10,marginTop:4},row:{paddingHorizontal:15,gap:0,justifyContent:'space-between'},empty:{alignItems:'center',paddingTop:70,paddingHorizontal:30},emptyTitle:{color:colors.textPrimary,fontFamily:fonts.displayMedium,fontSize:15,marginTop:12},emptyText:{color:colors.textMuted,fontFamily:fonts.body,fontSize:10.5,textAlign:'center',marginTop:6}});